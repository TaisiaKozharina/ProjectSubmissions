export interface IPerson {
	id: number,
    fname?: string,
    lname?: string,
    dob?: Date,
    country?: string,
    address?: string,
    email?: string,
    phone?: string,
    password?: string
}